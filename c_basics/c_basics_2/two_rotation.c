#include<stdio.h>
void main()
{
    char a[20],b[20],c[20];
    gets(a);
    gets(b);
    int k=0;
    int n=strlen(a);
    int m=strlen(b);
    for(int i=2;a[i]='\0';i++)
    {
        c[++k]=a[i];
        ans=strcmp(b,c);
    }

}
